[project]
name = "neurotrace-it"
version = "0.1.0"
description = "Trajectory- and layer-aware data selection for instruction tuning."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

